source "https://rubygems.org"

# github-pages bundles Jekyll + all plugins that GitHub Pages supports.
# This ensures your local environment matches what GitHub uses to build your site.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-remote-theme"
end
